.next/
node_modules/
.env
.env.local
.env.backup.*
.DS_Store
*.log
/uploads/
# Audio sinh ra bởi Voice Provider (vd ElevenLabs) — file runtime, không phải asset tĩnh.
/public/generated/
# DB dump có thể chứa dữ liệu hội thoại/private memory thật — không commit.
/backups/
# Project Agent JSON storage (Phase 7) — dữ liệu dự án của người dùng, không phải source code.
/data/
